name: Compile blueprint (fast)

on:
  push:
    branches:
      - main # Trigger on pushes to the default branch
    paths:
      - 'blueprint/**'
      - 'RootSystem/**'
      - 'lakefile.toml'
      - 'lean-toolchain'
  workflow_dispatch:        # Allow manual triggering of the workflow from the GitHub Actions interface

# Cancel previous runs if a new commit is pushed to the same PR or branch
concurrency:
  group: ${{ github.ref }}  # Group runs by the ref (branch or PR)
  cancel-in-progress: true  # Cancel any ongoing runs in the same group

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read          # Read access to repository contents
  pages: write            # Write access to GitHub Pages
  id-token: write         # Write access to ID tokens
  issues: write           # Write access to issues
  pull-requests: write    # Write access to pull requests

jobs:
  build_project:
    runs-on: ubuntu-latest
    name: Build project
    steps:
      - name: Cleanup to free disk space
        uses: jlumbroso/free-disk-space@54081f138730dfa15788a46383842cd2f914a1be # v1.3.0
        with:
          tool-cache: false
          android: true
          dotnet: false
          haskell: false
          large-packages: false
          docker-images: false
          swap-storage: false

      - name: Checkout project
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
        with:
          fetch-depth: 0 # Fetch all history for all branches and tags

      # 追加①：elan キャッシュ
      - name: Cache elan
        uses: actions/cache@v4
        with:
          path: ~/.elan
          key: ${{ runner.os }}-elan

      # 追加②：lake キャッシュ
      - name: Cache lake
        uses: actions/cache@v4
        with:
          path: |
            .lake
            build
          key: ${{ runner.os }}-lake-${{ hashFiles('lakefile.toml') }}

      # 追加③：doc-gen 用キャッシュ（最重要）
      - name: Cache docbuild
        uses: actions/cache@v4
        with:
          path: .lake/build/docbuild
          key: ${{ runner.os }}-docbuild-${{ hashFiles('lakefile.toml') }}

      # 追加④：Lean環境だけ先にセットアップ
      - name: Setup Lean
        uses: leanprover/lean-action@v1
        with:
          build: false

      # 修正：ここに移動（順序が重要）
      - name: Get mathlib cache
        run: lake exe cache get

      # 修正：軽量 build
      - name: Build project
        run: lake build

      # doc-gen（キャッシュ効くようになる）
      - name: Compile blueprint and documentation
        uses: leanprover-community/docgen-action@deed0cdc44dd8e5de07a300773eb751d33e32fc8
        with:
          blueprint: true
          homepage: home_page